!!!
%html

  %head
    %title
      = yield(:title) || "Untitled"
    %meta{"http-equiv"=>"Content-Type", :content=>"text/html; charset=utf-8"}/
    = stylesheet_link_tag "application"
    = javascript_include_tag "application"
    = csrf_meta_tag
    = yield(:head)

  %body
    / Main navigation fixed on top
    .navbar.navbar-fixed-top
      .navbar-inner
        .container
          %a.brand{ :href=> "#"} Study Quiz
          %ul.nav
            %li.active= link_to "Home", root_url
            %li= link_to "Terminology", terminologies_path
            %li= link_to "Questions", questions_path
          %ul.nav.pull-right
            %li.dropdown
              .dropdown-toggle
                = link_to "Profile", "#", 'data-toggle' => "dropdown"
                %b.caret
              %ul.dropdown-menu
                %li= link_to "Link", "#"
                %li= link_to "Link", "#"
                %li.divider
                %li= link_to "Link", "#"
                %li= link_to "Link", "#"

              /<b class="caret"></b>
              
    #container
      - flash.each do |name, msg|
        = content_tag :div, msg, :id => "flash_#{name}"


      - if show_title?
        %h1= yield(:title)

      = yield
